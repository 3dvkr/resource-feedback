<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head'); %>
</head>

<body class="container">

    <header>
        <%- include('../partials/header'); %>
    </header>

    <main>
        <div class="container">
            <h1>LRN</h1>
            <div class="grid">
                <% for ( let i = 0; i < feedbackDocs.length; i++ ) { %>
                    <%- include('../partials/card', { feedback: feedbackDocs[i] }); %>
                    <%- include('../partials/card', { feedback: feedbackDocs[i] }); %>
                    <%- include('../partials/card', { feedback: feedbackDocs[i] }); %>
                    <%- include('../partials/card', { feedback: feedbackDocs[i] }); %>
                    <%- include('../partials/card', { feedback: feedbackDocs[i] }); %>
                    <%- include('../partials/card', { feedback: feedbackDocs[i] }); %>
                <% } %>
            </div>
        </div>
    </main>

    <footer>
        <%- include('../partials/footer'); %>
    </footer>

</body>

</html>